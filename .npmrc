# Fix for npm optional dependencies bug with platform-specific binaries
# Ensures Rollup platform binaries are installed correctly in all environments
shamefully-hoist=true
legacy-peer-deps=false
# Force installation of all optional dependencies including platform binaries
install-strategy=hoisted
# Ensure platform-specific binaries are not skipped
platform-target=linux
